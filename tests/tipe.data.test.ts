describe('Data Type', function(){
    it('should must declare', function (){
        const name: string = "Andika Permana Putra";
        const balance: number = 123456;
        const isVip:boolean = true;

        console.info(name);
        console.info(balance);
        console.info(isVip);

        // name = 1100;
        // balance = "andika";
        // isVip = 100;
    })
})